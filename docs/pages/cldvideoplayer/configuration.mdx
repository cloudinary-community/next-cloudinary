import Head from 'next/head';

import OgImage from '../../components/OgImage';
import Table from '../../components/Table';

<Head>
  <title>CldVideoPlayer Configuration - Next Cloudinary</title>
  <meta name="og:title" content="CldVideoPlayer Configuration - Next Cloudinary" />
  <meta name="og:url" content={`https://next.cloudinary.dev/cldvideoplayer/configuration`} />
</Head>

<OgImage
  title="CldVideoPlayer Configuration"
  twitterTitle="CldVideoPlayer Configuration"
/>

# CldVideoPlayer Configuration

## General Props

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' },
    { id: 'example', title: 'Example' }
  ]}
  data={[
    {
      prop: 'className',
      type: 'string',
      default: '-',
      description: 'Additional class names added to the video container',
      example: () => (<code>"my-video-player"</code>)
    },
    {
      prop: 'height',
      type: 'string/number',
      default: '-',
      description: (<span><strong>Required</strong>: Player height</span>),
      example: () => (<code>1080</code>)
    },
    {
      prop: 'id',
      type: 'string',
      default: '-',
      description: 'Video instance ID, defaults to src value',
      example: () => (<code>"my-video"</code>)
    },
    {
      prop: 'logo',
      type: 'boolean/object',
      default: 'See Below',
      description: 'Logo to display in Player UI',
      example: 'See Logo Below'
    },
    {
      prop: 'onDataLoad',
      type: 'Function',
      default: '-',
      description: 'Triggered when video metadata is loaded',
      example: 'See Events Below'
    },
    {
      prop: 'onError',
      type: 'Function',
      default: '-',
      description: 'Triggered on video error',
      example: 'See Events Below'
    },
    {
      prop: 'onMetadataLoad',
      type: 'Function',
      default: '-',
      description: 'Triggered when video data is loaded',
      example: 'See Events Below'
    },
    {
      prop: 'onPause',
      type: 'Function',
      default: '-',
      description: 'Triggered on video pause',
      example: 'See Events Below'
    },
    {
      prop: 'onPlay',
      type: 'Function',
      default: '-',
      description: 'Triggered on video play',
      example: 'See Events Below'
    },
    {
      prop: 'onEnded',
      type: 'Function',
      default: '-',
      description: 'Triggered when video has ended play',
      example: 'See Events Below'
    },
    {
      prop: 'playerRef',
      type: 'Ref',
      default: '-',
      description: 'React ref to access Player instance',
      example: 'See Refs Below'
    },
    {
      prop: 'poster',
      type: 'string/object',
      default: '-',
      description: 'Customize the video\'s poster',
      example: 'See Poster Below'
    },
    {
      prop: 'src',
      type: 'string',
      default: '-',
      description: (<span><strong>Required</strong>: Video public ID</span>),
      example: () => (<code>"videos/my-video"</code>)
    },
    {
      prop: 'videoRef',
      type: 'Ref',
      default: '-',
      description: 'React ref to access video element',
      example: 'See Refs Below'
    },
    {
      prop: 'width',
      type: 'string/number',
      default: '-',
      description: (<span><strong>Required</strong>: Player width</span>),
      example: () => (<code>1920</code>)
    }
  ]}
/>



## Player visuals Props

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' },
    { id: 'example', title: 'Example' }
  ]}
  data={[
    {
      prop: 'aiHighlightsGraph',
      type: 'boolean',
      default: 'false',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'bigPlayButton',
      type: 'boolean/string',
      default: 'true',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'colors',
      type: 'object',
      default: 'See below',
      description: 'Player chrome colors',
      example: 'See Colors Below'
    },
    {
      prop: 'controlBar',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'controls',
      type: 'boolean',
      default: 'true',
      description: 'Show player controls',
      example: () => (<code>true</code>)
    },
    {
      prop: 'floatingWhenNotVisible',
      type: 'string',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'fluid',
      type: 'boolean',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'fontFace',
      type: 'string',
      default: '-',
      description: 'Player UI font. Uses Google Fonts.',
      example: () => (<code>"Source Serif Pro"</code>)
    },
    {
      prop: 'hideContextMenu',
      type: 'boolean',
      default: 'false',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'interactionAreas',
      type: 'any',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'playbackRates',
      type: 'array',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'playlistWidget',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'posterOptions',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'showJumpControls',
      type: 'boolean',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'showLogo',
      type: 'boolean',
      default: 'true',
      description: 'Show the Cloudinary logo on Player',
      example: () => (<code>false</code>)
    },
    {
      prop: 'seekThumbnails',
      type: 'boolean',
      default: 'true',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'videoJS',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    }
  ]}
/>

## Player Behavior Props

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' },
    { id: 'example', title: 'Example' }
  ]}
  data={[
    {
      prop: 'autoplay',
      type: 'string/boolean',
      default: '"never"',
      description: (
        <span>
          When, if, should the video automatically play. Note that if the value passed is a boolean then the value will be passed to <code>autoplay</code> but if the value passed is a string then the value will be passed to <code>autoplayMode</code>. See <code>autoplayMode</code> in <a href="https://cloudinary.com/documentation/video_player_api_reference#constructor_parameters">Video Player docs</a>
        </span>
      ),
      example: () => (<code>"on-scroll"/true</code>)
    },
    {
      prop: 'autoShowRecommendations',
      type: 'boolean',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'loop',
      type: 'boolean',
      default: 'false',
      description: 'Loop the video',
      example: () => (<code>true</code>)
    },
    {
      prop: 'maxTries',
      type: 'number',
      default: '3',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'muted',
      type: 'boolean',
      default: 'false',
      description: 'Load muted by default',
      example: () => (<code>true</code>)
    },
    {
      prop: 'pictureInPictureToggle',
      type: 'boolean',
      default: 'false',
      description: 'Uses browser\'s Picture-in-Picture API to add floating video UI.',
      example: () => (<code>true</code>)
    },
    {
      prop: 'playedEventPercents',
      type: 'array',
      default: '[25, 50, 75, 100]',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'playedEventTimes',
      type: 'array/null',
      default: 'null',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'playsinline',
      type: 'boolean',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'videoTimeout',
      type: 'number',
      default: '55000',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'withCredentials',
      type: 'boolean',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    }
  ]}
/>

## Video Config Props

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' },
    { id: 'example', title: 'Example' }
  ]}
  data={[
    {
      prop: 'chapters',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'preload',
      type: 'string',
      default: 'auto',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'publicId',
      type: 'string',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'sourceTransformation',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'sourceTypes',
      type: 'array',
      default: '-',
      description: 'Streaming format',
      example: () => (<code>['hls']</code>)
    },
    {
      prop: 'textTracks',
      type: 'object',
      default: '-',
      description: 'Captions or subtitles for the active video',
      example: 'See Text Tracks below'
    },
    {
      prop: 'transformation',
      type: 'object/array',
      default: '-',
      description: 'Transformations to apply to the video',
      example: () => (<code>{`{ width: 200, height: 200, crop: 'fill' }`}</code>)
    }
  ]}
/>

## Ads And Analytics Props

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' },
    { id: 'example', title: 'Example' }
  ]}
  data={[
    {
      prop: 'ads',
      type: 'object',
      default: '-',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'analytics',
      type: 'boolean',
      default: 'false',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    },
    {
      prop: 'allowUsageReport',
      type: 'boolean',
      default: 'true',
      description: (<span><strong>Check Cloudinary Video Player Api Docs</strong></span>),
      example: (<span><strong>Check Cloudinary Video Api Docs</strong></span>)
    }
  ]}
/>

Missing an option from the [Video Player docs](https://cloudinary.com/documentation/video_player_api_reference) you'd like to see? [Create an issue](https://github.com/cloudinary-community/next-cloudinary/issues/new?assignees=&labels=Type%3A+Feature&template=feature_request.md&title=%5BFeature%5D+)!

## Customization

### Logo

The `logo` prop gives the option to customize the player's logo.

`logo` defaults to `true`, showing the Cloudinary logo and linking to https://cloudinary.com when clicked.

When `logo` is set to `false`, no logo will be displayed.

To customize the logo, the following options are available in the form of an object:

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' }
  ]}
  data={[
    {
      prop: 'imageUrl',
      type: 'string',
      default: '-',
      description: 'Image URL for player logo.'
    },
    {
      prop: 'onClickUrl',
      type: 'string',
      default: '-',
      description: 'URL to browse to on logo click.'
    }
  ]}
/>


### Poster

The `poster` prop optionally takes a string or object to customize the generated poster.

When passing a string, you can either pass a Cloudinary Public ID or a remote URL to rendered the desired image.

When passing an object, use same configuration and API as [getCldImageUrl](/getcldimageurl/configuration) to customize the image.
You can either specify a `src` option with a custom public ID or omit the `src`, which will use the video's
ID to render an automatically generated preview image.

[View examples](/cldvideoplayer/examples#poster).

### Text Tracks

The `textTracks` prop allows you to add captions or subtitles to the player.

Each Text Track is an object containing details about where the captions or subtitles should
be loaded from as well as any customization of that track.

**Examples**

```jsx
textTracks={{
  captions: {
      label: 'English',
      language: 'en',
      default: true,
      url: 'https://res.cloudinary.com/<Cloud Name>/raw/upload/<FileName>.vtt'
    }
  }
}
```

Learn more on the [Video Player Reference](https://cloudinary.com/documentation/video_player_api_reference#text_tracks) or
the [Subtitles and Captions guide](https://cloudinary.com/documentation/video_player_customization#subtitles_and_captions).


### Theme Colors

The `colors` prop takes an object that can control what colors are used in the player:

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' }
  ]}
  data={[
    {
      prop: 'accent',
      type: 'string',
      default: (<code>"#FF620C"</code>),
      description: 'Seek bar, volume control and for highlighting interactions.'
    },
    {
      prop: 'base',
      type: 'string',
      default: (<code>"#000000"</code>),
      description: 'Player controls bar, information bar, central play button, and right-click context menu.'
    },
    {
      prop: 'text',
      type: 'string',
      default: (<code>"#FFFFFF"</code>),
      description: 'All the text and icons that are present within the video player UI.'
    }
  ]}
/>

Learn more about the color scheme options and how they're used [on the Cloudinary docs](https://cloudinary.com/documentation/video_player_customization#color_scheme).

### Localization

You can add custom localization options to control the languages of the player provides labels in.

<Table
  columns={[
    { id: 'prop', title: 'Prop Name' },
    { id: 'type', title: 'Type' },
    { id: 'default', title: 'Default' },
    { id: 'description', title: 'Description' }
  ]}
  data={[
    {
      prop: 'language',
      type: 'string',
      default: '-',
      description: 'The language of the player\'s labels.'
    },
    {
      prop: 'languages',
      type: 'object',
      default: '-',
      description: 'Language configuration and label overrides.'
    }
  ]}
/>

Uses Video.js JSON format for localization customization: https://videojs.com/guides/languages/#json-format

## Player APIs

### Event Handlers

The event props allow you to pass in a function that is called whenever the associated event occurs.

For instance, in order to trigger an event whenever a video is paused:

```jsx copy
<CldVideoPlayer onPause={({ player }) => {
  const duration = player.duration();
}} ... />
```

### Refs

The `playerRef` and `videoRef` props give you the ability to pass in your own ref to gain access to both the Player instance as well as the HTML video element on which the player is mounted.

To do this, create a new Ref instance and pass that in as the value of the prop:

```jsx copy
const myVideoRef = useRef();
...
<CldVideoPlayer videoRef={myVideoRef} ... />
```

## Advanced

### Configuration & Delivery

<Table
  columns={[
    {
      id: 'prop',
      title: 'Prop'
    },
    {
      id: 'type',
      title: 'Type'
    },
    {
      id: 'default',
      title: 'Default'
    },
    {
      id: 'example',
      title: 'Example'
    },
    {
      id: 'more'
    }
  ]}
  data={[
    {
      prop: 'config',
      type: 'object',
      default: '-',
      example: () => (<code>{`{ url: { cloudName: 'spacejelly' } }`}</code>),
      more: () => (<a className="whitespace-nowrap" href="https://cloudinary.com/documentation/cloudinary_sdks#configuration_parameters">More Info</a>)
    },
    {
      prop: 'cname',
      type: 'string',
      example: () => (<code>{`spacejelly.dev`}</code>),
      more: () => (<a className="whitespace-nowrap" href="https://cloudinary.com/documentation/cloudinary_sdks#configuration_parameters">More Info</a>)
    },
    {
      prop: 'privateCdn',
      type: 'boolean',
      example: () => (<code>{`true`}</code>),
      more: () => (<a className="whitespace-nowrap" href="https://cloudinary.com/documentation/cloudinary_sdks#configuration_parameters">More Info</a>)
    },
    {
      prop: 'secureDistribution',
      type: 'string',
      example: () => (<code>{`spacejelly.dev`}</code>),
      more: () => (<a className="whitespace-nowrap" href="https://cloudinary.com/documentation/cloudinary_sdks#configuration_parameters">More Info</a>)
    },
    {
      prop: 'queryParams',
      type: 'string/object',
      example: () => (<code>{`{myParam: 'value'}`}</code>),
      more: () => (<a className="whitespace-nowrap" href="https://cloudinary.com/documentation/cloudinary_sdks#configuration_parameters">More Info</a>)
    },
  ]}
/>

#### `config`

Allows configuration for the Cloudinary environment.

**Examples**

```jsx copy
config={{
  cloud: {
    cloudName: '<Your Cloud Name>',
  }
}}
```

#### `cname`

The custom domain name (CNAME) to use for building URLs, requires secure: false.

**Examples**

```jsx copy
cname="spacejelly.dev"
```

#### `privateCdn`

Set this parameter to true if you are an Advanced plan user with a private CDN distribution

**Examples**

```jsx copy
privateCdn={true}
```

#### `secureDistribution`

The custom domain name (CNAME) to use for building URLs, requires secure: true.

**Examples**

```jsx copy
secureDistribution="spacejelly.dev"
```

#### `queryParams`

Query parameters to append to video URL.

**Examples**

```jsx copy
queryParams={{
  myParam: 'value'
}}
```